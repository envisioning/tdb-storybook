import React, { PropTypes } from 'react'
import { Button } from 'react-bootstrap';

const MyComponent = (props) => {
  return (
    <Button bsStyle="success">My Button</Button>
  )
}

export default MyComponent
